{
  "name": "github-commit-status",
  "version": "1.0.0",
  "scripts": {
    "build": "tsc",
    "test": "vitest --run",
    "pack": "npm run build && ncc build lib/action.js -o dist"
  },
  "dependencies": {
    "@actions/core": "^1.10.1",
    "@actions/github": "^6.0.0"
  },
  "devDependencies": {
    "typescript": "^5.2.2",
    "vitest": "^0.34.6",
    "@vercel/ncc": "^0.38.1"
  }
}
